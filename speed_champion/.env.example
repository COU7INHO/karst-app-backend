# Django Environment Configuration
# Copy this file to .env and update with your values

# Environment: development or production
DJANGO_ENV=development

# Django Secret Key (REQUIRED in production)
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-#j^x7(knk@jt!qjwj=z(iafc7j5um0ujm9v2wox7onl=1^dbpw

# Database Configuration (Production only - PostgreSQL)
DB_NAME=karts_db
DB_USER=karts_user
DB_PASSWORD=your-secure-password-here
DB_HOST=localhost
DB_PORT=5432

# Static and Media Files (Production)
STATIC_ROOT=/path/to/staticfiles
MEDIA_ROOT=/path/to/media

# Logging (Production)
LOG_FILE=/path/to/django.log
